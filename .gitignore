# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Contains real submitter data (names, comments, addresses) — even
# though it's already publicly visible on the live map, don't also
# commit a portable dump of it.
supabase/SEED_REAL_DATA.sql
supabase/RUN_IN_NEW_SUPABASE_PROJECT.sql
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
